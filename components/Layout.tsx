
import React from 'react';

// Deprecated in favor of the new Simulator Switcher in App.tsx
export const Layout: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};
